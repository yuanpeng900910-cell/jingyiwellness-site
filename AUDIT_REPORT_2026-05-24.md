# 京颐养方 UI/UX/移动端/性能/可访问性审查（参考 Vercel Web Interface Guidelines 方向）

> 范围：仅基于当前仓库前端代码静态审查（`index.html` / `css/style.css` / `js/main.js`），不改代码。

## 高优先级

1. **移动端首屏 LCP 风险偏高（大图 + 多层覆盖）**
   - 首屏直接加载 `hero-woman.jpg`（1600x900，`fetchpriority="high"`），在手机弱网下首屏成本高。建议提供 AVIF/WebP + `srcset/sizes`，并使用针对移动端更小裁剪图。 
   - Hero 还有多层渐变和动态装饰，建议控制首屏视觉层数，把复杂视觉推迟到首屏后。

2. **筛选区在手机端展开后占屏明显，影响产品浏览起点**
   - 移动端 `filter-panel` + `mobile-filter-more` 展开后内容块较长，可能把首屏产品卡完全挤出。建议改为「底部抽屉 / 半屏弹层」或 sticky 精简栏（只保留关键筛选+结果数）。

3. **搜索+筛选状态缺少“清空/重置”一键动作，用户容易陷入无结果困境**
   - 目前空状态只提示“尝试切换/清空关键词”，但没有明确重置按钮。建议在空状态与筛选头部都提供“清空全部筛选”按钮，并显示当前 active 条件摘要。

4. **弹窗可访问性不足（焦点陷阱/返回焦点一致性不完整）**
   - 弹窗打开时会聚焦关闭按钮，但未实现焦点圈定（Tab 可穿透到背景）。
   - 茶详情弹窗关闭后未把焦点返回触发按钮（联系弹窗有返回逻辑，茶弹窗没有），会影响键盘与读屏体验。

5. **按钮命中面积存在不足（移动端）**
   - 多处按钮仍低于 44px（如筛选展开按钮 38px、茶详情按钮 38px、关闭按钮 34px），不符合移动端触控舒适区建议。建议统一最小点击高度≥44px。

6. **导航/头部信息密度较高，手机端顶部可用空间拥挤**
   - Logo、语言选择、汉堡按钮在窄屏并存，且 nav 菜单展开覆盖式较重。建议简化头部优先级（语言切换下沉/折叠），降低首屏干扰。

## 中优先级

1. **产品筛选“死组合”已部分自愈，但反馈仍不透明**
   - 逻辑会在无结果时自动回退 `scene=全部`，避免部分死组合；但用户可能不理解系统“悄悄改了条件”。建议增加轻提示（如“已自动放宽场景筛选”）。

2. **产品卡信息密度偏高，移动端阅读负担大**
   - 卡片含标题、体质/人群、规格、配料 details、简介、场景标签，文本层级偏重。建议移动端默认只显示 2~3 条关键信息，其余折叠到“查看详情”。

3. **对比度局部边缘风险**
   - 部分浅底+浅字组合（例如 muted 文本、某些标签底色）在户外或低亮度下可读性可能下降。建议统一做 AA 对比度基线校验（正文至少 4.5:1）。

4. **搜索交互可理解性还可增强**
   - 输入框 label 为“人群搜索”，但实际匹配范围覆盖产品名/配方/场景等。建议文案改为“关键词搜索”，并显示已搜索字段提示。

5. **首屏之外图片加载策略可更精细**
   - 目前主要靠 `loading="lazy"`，但可进一步用 `fetchpriority="low"`、更细的 `sizes`，以及按视口分组预取，减少中端机滚动抖动。

## 低优先级

1. **样式表重复定义较多，维护成本上升**
   - `style.css` 出现多段重复/覆盖式定义（如 series 轮播相关块多次出现）。短期可用，长期建议做样式分层与去重，降低回归风险。

2. **列表与轮播控件状态提示可再强化**
   - 轮播圆点/箭头已有，但可增加“当前第 N 页 / 共 M 页”的辅助文案，提升可理解性与读屏友好度。

3. **空状态可行动性可增强**
   - 当前仅文本提示。建议增加一键清空、推荐筛选词或热门组合入口，缩短恢复路径。

## 对 10 个重点检查项的结论速览

1. 手机端首屏加载速度：**有风险（高优先级）**
2. 移动端导航和筛选区占屏：**有明显改进空间（高优先级）**
3. 产品筛选无结果死组合：**逻辑已部分规避，但反馈不足（中优先级）**
4. 产品卡片阅读体验：**信息密度偏高（中优先级）**
5. 图片 lazy loading 和首屏资源：**首屏重、后续可再优化（高/中）**
6. 按钮点击区域：**多个控件偏小（高优先级）**
7. 搜索框和筛选交互清晰度：**中等，需语义与重置机制增强（中/高）**
8. 弹窗手机端可用性：**基本可用，但无焦点陷阱/返回焦点不完整（高优先级）**
9. 字号、行高、间距移动端适配：**总体可用，局部可收敛信息密度（中）**
10. 对比度和可读性：**整体尚可，局部文本需 AA 复核（中）**
