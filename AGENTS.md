# AGENTS.md — Codex 省额度工作规则

## 1. 先读后改
开始前先阅读相关文件，不要全项目扫描。只查看与任务直接相关的文件。

## 2. 小步修改
每次只完成一个明确目标。禁止一次性重构整个页面、样式或架构。

## 3. 不做无关改动
不要修改无关文件、注释、格式、命名和已有文案，除非任务明确要求。

## 4. 先给方案，再动手
涉及 3 个以上文件、布局大改、组件重构时，先输出简短方案，等待确认后再实施。

## 5. 优先复用
优先复用现有组件、CSS 变量、配色、布局，不新增复杂依赖。

## 6. 不主动装包
未经确认，不安装新依赖、不更换框架、不引入大型库。

## 7. 精准搜索
不要反复全文搜索。先用文件名、组件名、页面路径定位，再读取必要片段。

## 8. 每轮只跑必要验证
小改只跑对应页面预览或局部检查；不要每次都跑完整测试或完整构建。

## 9. 报告要短
完成后只汇报：改了什么、涉及哪些文件、如何验证。不要长篇解释。

## 10. 发现不确定先停
遇到素材缺失、需求冲突、实现路径不确定时，先提问，不要猜。

## 11. 视觉任务按设计目标执行
网页视觉修改以“高级、留白、草本绿、浅灰/白底、适配手机”为默认风格，不擅自改变品牌方向。

## 12. 保持可回退
每次修改尽量小而清晰，避免大面积改动导致难以回退。

## 项目默认风格
- 品牌：京颐养方
- 风格：中式养生、现代留白、草本感、高级但不厚重
- 主色：草本绿、白色、浅灰
- 辅助色：咖啡色、浅绿色、金色
- 页面优先级：手机端优先，其次桌面端
- 设计原则：清晰、克制、留白充足、产品信息易读

## 推荐任务格式
```text
请严格遵守 AGENTS.md。

目标：只修改【具体页面/模块】。
范围：只改【具体文件】。
不要改：【不希望改动的内容】。
要求：【设计/交互/文案要求】。
验证：只做必要预览或局部检查，不要跑完整构建。
完成后：只汇报修改文件和验证结果。
```
